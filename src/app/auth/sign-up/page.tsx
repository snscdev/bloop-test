import { CustomSignUpView } from 'src/auth/view/custom';

// ----------------------------------------------------------------------

export const metadata = { title: 'Registrarse' };

export default function Page() {
  return <CustomSignUpView />;
}
