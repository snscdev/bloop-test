import { CustomResetPasswordView } from 'src/auth/view/custom';

// ----------------------------------------------------------------------

export const metadata = { title: 'Restablecer contraseña' };

export default function Page() {
  return <CustomResetPasswordView />;
}
