import { CustomAuth0LoginView } from 'src/auth/view/custom';

// ----------------------------------------------------------------------

export const metadata = { title: 'Iniciar sesión con Google' };

export default function Page() {
  return <CustomAuth0LoginView />;
}
