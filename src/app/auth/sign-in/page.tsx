import { CustomValidateEmailView } from 'src/auth/view/custom';

// ----------------------------------------------------------------------

export const metadata = { title: 'Iniciar sesión' };

export default function Page() {
  return <CustomValidateEmailView />;
}
