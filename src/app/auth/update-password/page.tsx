import { CustomUpdatePasswordView } from 'src/auth/view/custom';

// ----------------------------------------------------------------------

export const metadata = { title: 'Actualizar contraseña' };

export default function Page() {
  return <CustomUpdatePasswordView />;
}
